name: News Quality Quarantine Disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  quarantine-disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "Automatic homepage hiding is disabled. Quality checks must not remove live news cards without a separate rebuild impact review."
