name: Rebuild Home From Quality Metadata Disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "Automatic removal of home cards is disabled. Bad articles must be reviewed and removed explicitly, not by hiding the whole home index."
